export enum ActivityCategory {
  ORDER = 'order',
  DRIVER = 'driver',
  TEAM = 'team',
  API_KEY = 'api_key',
  SYSTEM = 'system',
}

export enum ActivitySeverity {
  INFO = 'info',
  WARNING = 'warning',
  CRITICAL = 'critical',
}
